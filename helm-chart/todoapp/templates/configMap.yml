apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Chart.Name }}-config
  namespace: {{ .Values.common.namespace }}
data:
  {{- range $key, $value := .Values.todoapp.configs }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}